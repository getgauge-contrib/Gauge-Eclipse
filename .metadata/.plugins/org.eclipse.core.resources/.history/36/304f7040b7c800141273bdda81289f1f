grammar io.getgauge.Spec 
import "http://www.eclipse.org/emf/2002/Ecore" as ecore

generate spec "http://www.getgauge.io/Spec"

Model:
	definitions+=AbstractEntity*
;

AbstractEntity:
	(Spec | Scenario | Step) | Comment
;

Spec:
	'# ' name+=(NOT_NL)+ SINGLE_NL+
;

Scenario:
	'## ' name+=(NOT_NL)+ SINGLE_NL+
;

Step:
	{Step}
	'* ' (((params+=StaticParam) | WORD ) NOT_NL?)+ SINGLE_NL+
;

Comment:
	text+=(NOT_NL)+ SINGLE_NL+
;

terminal SINGLE_NL:
	'\r'? '\n'
;

StaticParam:
	value=STATIC_PARAM
;

terminal STATIC_PARAM:
	'"' -> '"'
;

terminal WORD: ('a'..'z'|'A'..'Z'|'_') ('a'..'z'|'A'..'Z'|'_'|'0'..'9')*;

terminal NOT_NL:
	!('\r' | '\n')
;
